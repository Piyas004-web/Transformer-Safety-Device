# Transformer-Safety-Device
The aim of this project is to protect the transformer during overloading. It will also have sensor to sense the Overload current of transformer and cut off the extra load. Transformer Protec device are used to reducing the extra load automatically, and transformer can be protected. 
